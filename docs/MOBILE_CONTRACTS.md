# Steeple Mobile — Internal Contracts (moved)

> **Moved 2026-08-05 to [`docs/contracts/mobile.md`](contracts/mobile.md)**, content unchanged
> and section numbering preserved — "MOBILE_CONTRACTS §n" citations in `/mobile` code and
> `mobile/README.md` resolve to the same §n there.
>
> That file is the owning location for `/mobile`'s in-app seams (interfaces, routes,
> providers, shared widgets). Wire shapes live in the sibling seam files; the full index is
> `docs/contracts/README.md`.
