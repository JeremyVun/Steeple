@model Steeple.Admin.ViewModels.Admin.AdminWorkspaceViewModel
@{
    Layout = "_Layout";
    ViewData["Robots"] = "noindex,nofollow";
}

<section class="admin-page wrap">
    @await Html.PartialAsync("_AdminNav", Model)

    <section class="admin-panel" id="admin-analytics" aria-labelledby="admin-analytics-heading">
        <div class="admin-panel-header">
            <div>
                <p class="admin-eyebrow">Events</p>
                <h2 id="admin-analytics-heading">Analytics stream</h2>
            </div>
        </div>
        @await Html.PartialAsync("_AnalyticsPanel", Model)
    </section>
</section>
