{
  "AllowedHosts": "*",
  "Admin": {
    // CSP img-src source list for listing photos (the media origin — API or CDN). Narrow this to
    // the exact origin in deployment: "https://media.steeple.example".
    "MediaImageOrigins": "https:",
    // Internal API origin used only when a room photo has a local-disk media/... path.
    "MediaProxyOrigin": "http://api:8080"
  }
}
