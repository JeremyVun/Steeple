# Steeple — crawl policy (docs/SEO.md item 2).
#
# Everything here is meant to be found: discovery is the demand-side cold start,
# and every listing is a page somebody might share. There is nothing to keep a
# crawler out of on this surface — v1's disallow rules were about its faceted
# /search URLs and its id→slug redirects, and this app has neither.
#
# The sitemap lives at /sitemap.xml, which the API renders
# (GET /api/v1/sitemap.xml) and the edge aliases — see nginx.conf.

User-agent: *
Allow: /

Sitemap: /sitemap.xml
