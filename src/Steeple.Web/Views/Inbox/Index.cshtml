@model InboxViewModel

<section class="inbox-page wrap">
    <header class="inbox-head">
        <h1>Your requests</h1>
        @if (Model.IsProvider)
        {
            <a class="head-crosslink" href="~/manage/applications">Requests for your spaces →</a>
        }
    </header>

    <partial name="_ApplicationList" model="Model" />
</section>
